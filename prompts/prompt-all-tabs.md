# Further implementations

The @dashboard.jpg gives an idea of what dashboard GUI should look like.
The implementation for key indicatiors and the mock data looks good.

Followng similar structure, each of the other tabs have to be implemented.

# Do this
- Spin up worktrees for each tab that is not yet implemented (Such as "Inflation", "Employment", etc.)
- imagine what should be an appropriate graph or chart for the tab
- Update the footer as 'Data mocked based on Federal Reserve Economic Data (FRED)
- check for merge conflicts, resolve, and merge code from each of the worktree
- name the worktree branch according to the tab being implemented
- each worktree should have a last step of making mock data similar to "Key indicators" tab